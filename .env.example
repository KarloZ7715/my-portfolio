# Public URL of the deployed contact-form Cloudflare Worker.
# Not a secret — it's baked into the client bundle at build time.
VITE_CONTACT_API_URL=https://karloz-contact-worker.<your-subdomain>.workers.dev
